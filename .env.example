# 本站目前不需要任何環境變數。
#
# 會員系統（LINE 登入、session token、Supabase 讀寫）與檢驗報告查詢已於
# 「移除健康 App」時整批下線，預約諮詢改為前端直接帶入 LINE 官方帳號的
# 對話框，不經過伺服器、不寫資料庫。因此本站沒有任何機密可外洩。
#
# 部署時 Vercel 專案上若還留著下列舊變數，建議一併刪除，避免誤以為它們
# 仍在生效 —— 尤其 SUPABASE_SERVICE_ROLE_KEY 會繞過 RLS，不該繼續掛著：
#
#   VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY
#   SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY
#   VITE_LIFF_ID
#   LINE_LOGIN_CHANNEL_ID / LINE_LOGIN_CHANNEL_SECRET
#   LINE_CHANNEL_ACCESS_TOKEN
#   LIS_ENDPOINT / MOCK_LIS_PORT
#   PUBLIC_SITE_URL
#   SESSION_TOKEN_SECRET
#
# 日後 Phase 2 若恢復檢驗報告查詢，請連同雙因子驗證一起重新設計，屆時再
# 把需要的變數加回這個檔案（見 docs/ROADMAP.md）。
